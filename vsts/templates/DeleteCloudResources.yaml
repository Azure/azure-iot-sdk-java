steps:
- task: AzureCLI@2
  displayName: Delete deployed cloud test resources
  condition: always()
  inputs:
    azureSubscription: 'iot hub sdk service connection'
    scriptType: 'pscore'
    scriptLocation: 'inlineScript'
    inlineScript: |
      Write-Host "Deleting resource group with name "
      Write-Host $(deployCloudTestResources.RESOURCE_GROUP_NAME)
      az deployment group delete --resource-group $(deployCloudTestResources.RESOURCE_GROUP_NAME) --name $(deployCloudTestResources.DEPLOYMENT_NAME) --output none --only-show-errors
      az group delete --name $(deployCloudTestResources.RESOURCE_GROUP_NAME) --y
